# Local dev: leave empty to use Vite proxy (/api -> localhost:8080)
# VITE_API_URL=

# Production (Cloudflare Pages): set in Pages → Settings → Environment variables
# VITE_API_URL=https://YOUR-BACKEND.onrender.com/api
